

/*
// log-message.component.ts
import 'aframe  '

AFRAME.registerComponent('log-message', {
  schema: {
    message: { type: 'string', default: 'Hello, A-Frame!' },
  },

  init: function () {
    console.log(this.data.message);

    // Opcional: Mostrar mensaje en pantalla como texto
    const textElement = document.createElement('a-text');
    textElement.setAttribute('value', this.data.message);
    textElement.setAttribute('color', 'black');
    textElement.setAttribute('position', '0 1 0');
    this.el.appendChild(textElement);
  },
});
*/